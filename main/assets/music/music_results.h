#pragma once
#include <cstdint>

// Généré à partir de music_results.pmf via tools/pmf_to_c.py (cf. racine du projet).
// Remplace music_results.cpp par la sortie du script une fois ton fichier .pmf prêt.
extern const uint8_t music_results_pmf[];
extern const unsigned int music_results_pmf_len;
