#pragma once
#include <cstdint>

// Généré à partir de jingle_lastlap.pmf via tools/pmf_to_c.py (cf. racine du projet).
// Remplace jingle_lastlap.cpp par la sortie du script une fois ton fichier .pmf prêt.
extern const uint8_t jingle_lastlap_pmf[];
extern const unsigned int jingle_lastlap_pmf_len;
