#pragma once
#include <cstdint>

// Généré à partir de jingle_finish.pmf via tools/pmf_to_c.py (cf. racine du projet).
// Remplace jingle_finish.cpp par la sortie du script une fois ton fichier .pmf prêt.
extern const uint8_t jingle_finish_pmf[];
extern const unsigned int jingle_finish_pmf_len;
