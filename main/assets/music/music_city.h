#pragma once
#include <cstdint>

// Généré à partir de music_city.pmf via tools/pmf_to_c.py (cf. racine du projet).
// Remplace music_city.cpp par la sortie du script une fois ton fichier .pmf prêt.
extern const uint8_t music_city_pmf[];
extern const unsigned int music_city_pmf_len;
